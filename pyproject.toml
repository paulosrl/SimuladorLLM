[project]
name = "simuladorllm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "matplotlib",
    "streamlit",
    "plotly",
    "google-generativeai",
    "python-dotenv",
]